<?php
if (!defined('WP_UNINSTALL_PLUGIN')) {
    exit;
}

// Прибрати, якщо не треба видаляти дані:
//global $wpdb;
//$table = $wpdb->prefix . 'alb_hpp_payments';
//$wpdb->query("DROP TABLE IF EXISTS `$table`");
